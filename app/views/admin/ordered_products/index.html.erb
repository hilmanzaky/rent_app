<h1>Listing ordered_products</h1>

<table>
  <tr>
    <th>Order</th>
    <th>Product</th>
    <th>Description</th>
    <th>Sub total</th>
    <th>Qty</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ordered_products.each do |ordered_product| %>
  <tr>
    <td><%= ordered_product.order_id %></td>
    <td><%= ordered_product.product_id %></td>
    <td><%= ordered_product.description %></td>
    <td><%= ordered_product.sub_total %></td>
    <td><%= ordered_product.qty %></td>
    <td><%= link_to 'Show', ordered_product %></td>
    <td><%= link_to 'Edit', edit_ordered_product_path(ordered_product) %></td>
    <td><%= link_to 'Destroy', ordered_product, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ordered product', new_ordered_product_path %>
